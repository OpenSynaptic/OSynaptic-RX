@PACKAGE_INIT@

# OSynaptic-RX CMake package config
# Generated by CMakePackageConfigHelpers.
#
# Usage in downstream projects:
#
#   find_package(osrx REQUIRED)
#   target_link_libraries(myapp PRIVATE osrx::osrx)
#
# Optional components not yet defined; all targets are imported via the
# main include below.

include("${CMAKE_CURRENT_LIST_DIR}/osrxTargets.cmake")

check_required_components(osrx)
